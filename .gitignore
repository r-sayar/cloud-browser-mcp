# BrowserOS Linux build — fetch from BrowserOS releases (~280 MB, too big for git)
BrowserOS.AppImage

# A clone of the BrowserOS source repo — not part of this project
browseros/

# Persistent profiles (cookies, history, saved passwords) — never commit
data/

# Profile snapshots
profile-*.tgz

# macOS / Python / Node noise
.DS_Store
__pycache__/
*.pyc
node_modules/

# Editor
.vscode/
.idea/

# Python venvs (one shared at repo root, plus the legacy gmail_mcp/.venv)
.venv/
venv/
gmail_mcp/.venv/

# Backups created by config-edit scripts
*.backup-*

# Local environment overrides
.env
.env.local

# Per-site MCP servers — kept local only. gmail_mcp/ remains as the template
# (referenced by docs/PROTOCOL.md); smart_browseros_mcp/ stays as the affordance layer.
amazon_mcp/
calendly_mcp/
canvas_mcp/
claude_ai_mcp/
fu_berlin_mcp/
linkedin_mcp/
luma_mcp/
notion_mcp/
outlook_mcp/
pubmed_mcp/
skyscanner_mcp/
wikipedia_mcp/
youtube_mcp/
zoom_mcp/

# Unrelated working dir
blast-&-pam/
